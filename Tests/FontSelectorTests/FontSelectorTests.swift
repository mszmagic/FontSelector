import XCTest
@testable import FontSelector

final class FontSelectorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FontSelector().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
